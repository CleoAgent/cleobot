services:
  - type: web
    name: openclaw
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "8080"
      - key: SETUP_PASSWORD
        sync: false
      - key: CLEOBOT_STATE_DIR
        value: /data/.openclaw
      - key: CLEOBOT_WORKSPACE_DIR
        value: /data/workspace
      - key: CLEOBOT_GATEWAY_TOKEN
        generateValue: true
    disk:
      name: openclaw-data
      mountPath: /data
      sizeGB: 1
