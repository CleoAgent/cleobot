# =============================================================================
# CLEO Bot Environment Variables
# =============================================================================
#
# SETUP OPTIONS:
#
# Option 1: Local .env file (development)
#   cp .env.example .env
#   nano .env  # Fill in your credentials
#
# Option 2: Doppler (recommended for production)
#   doppler setup --project cleobot --config prd
#   doppler run -- docker compose up -d
#
# =============================================================================

# === CLEOBOT Core ===
# Config and workspace directories (container paths)
CLEOBOT_CONFIG_DIR="/root/.cleobot"
CLEOBOT_WORKSPACE_DIR="/root/.cleobot/workspace"

# Gateway authentication (generate with: openssl rand -hex 24)
CLEOBOT_GATEWAY_TOKEN=""

# Network binding
CLEOBOT_GATEWAY_PORT=18789
CLEOBOT_BRIDGE_PORT=18790
CLEOBOT_GATEWAY_BIND=lan    # Options: loopback, lan, any

# Docker image
CLEOBOT_IMAGE=cleobot:local

# === AI Providers (at least one required) ===
# Anthropic Claude (primary)
ANTHROPIC_API_KEY=

# OpenAI (optional, for embeddings/fallback)
OPENAI_API_KEY=

# Google Gemini (optional)
GOOGLE_API_KEY=

# === OAuth Subscriptions (optional) ===
# For Anthropic Max subscription usage tracking
CLAUDE_AI_SESSION_KEY=
# CLAUDE_WEB_COOKIE=  # Alternative: full cookie header

# === Web Search ===
BRAVE_API_KEY=

# === Messaging Channels ===
TELEGRAM_BOT_TOKEN=
DISCORD_BOT_TOKEN=

# === Services ===
# AgentMail (AI email)
AGENTMAIL_API_KEY=
AGENTMAIL_EMAIL=

# Moltbook (AI social)
MOLTBOOK_API_KEY=

# === GitHub (for code operations) ===
GITHUB_PAT=
GITHUB_USERNAME=

# =============================================================================
# SECURITY NOTES:
# - Never commit .env with real credentials
# - Use Doppler or similar secrets manager for production
# - Restrict CLEOBOT_GATEWAY_BIND to 'loopback' if not using external access
# - Generate strong gateway tokens (at least 24 bytes hex)
# =============================================================================
